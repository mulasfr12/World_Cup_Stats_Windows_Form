<UserControl x:Class="WorldCupWPF.PlayerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Width="63" Height="66">
    <Border BorderBrush="Black" BorderThickness="1" CornerRadius="5" Background="LightGreen" Margin="0,0,0,3" >
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top" Height="64" Width="66" Margin="0,5,0,0">
            <Image Source="{Binding Image}" Width="32" Height="22"/>
            <TextBlock Text="{Binding Name}" FontSize="12" FontWeight="Bold" TextAlignment="Center" Margin="0,5" Height="12"/>
            <TextBlock Text="{Binding ShirtNumber}" FontSize="10" TextAlignment="Center" />
        </StackPanel>
    </Border>
</UserControl>
